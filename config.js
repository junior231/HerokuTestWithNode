const config = {
    uname: 'root',
    upass: 'root', // windows users leave this blank
    port: 8889, // windows / linux 3306
    host: 'localhost',
    db: 'bizcard'
}

// make this a public file our app can reas
module.exports = config;